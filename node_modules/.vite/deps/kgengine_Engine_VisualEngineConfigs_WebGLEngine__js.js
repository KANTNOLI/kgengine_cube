import {
  BasicShadowMap,
  PCFShadowMap,
  PCFSoftShadowMap,
  WebGLRenderer
} from "./chunk-VBOIYXFH.js";

// node_modules/kgengine/Engine/VisualEngineConfigs/WebGLEngine.ts
var WebGLEngine = (quality = {
  antialias: true,
  precision: "mediump",
  powerPrfrnc: "default",
  depth: true,
  shadowOn: true,
  shadowMap: "normal"
}, sizes = {
  width: window.innerWidth,
  height: window.innerHeight
}, css3On = true) => {
  const renderer = new WebGLRenderer({
    antialias: quality.antialias || true,
    precision: quality.precision || "mediump",
    powerPreference: quality.powerPrfrnc || "default",
    depth: quality.depth || true
  });
  renderer.setSize(sizes.width, sizes.height);
  if (quality.shadowOn != void 0 ? quality.shadowOn : true) {
    renderer.shadowMap.enabled = true;
    switch (quality.shadowMap != void 0 ? quality.shadowMap : "low") {
      case "low":
        renderer.shadowMap.type = BasicShadowMap;
        break;
      case "hard":
        renderer.shadowMap.type = PCFSoftShadowMap;
        break;
      default:
        renderer.shadowMap.type = PCFShadowMap;
        break;
    }
  }
  if (css3On) {
    renderer.domElement.style.position = "absolute";
    renderer.domElement.style.zIndex = "0";
    renderer.domElement.style.top = "0";
  }
  document.body.appendChild(renderer.domElement);
  return renderer;
};
export {
  WebGLEngine
};
//# sourceMappingURL=kgengine_Engine_VisualEngineConfigs_WebGLEngine__js.js.map
