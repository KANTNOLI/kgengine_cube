import {
  PerspectiveCamera
} from "./chunk-64CEIPPX.js";

// node_modules/kgengine/Engine/Cameras/DefaultCameraSettings.ts
var DefaultCameraSettings = (position = { x: 0, y: 0.25, z: 1 }, perspective = {
  fov: 75,
  aspect: window.innerWidth / window.innerHeight,
  near: 0.01,
  far: 1e3
}) => {
  const camera = new PerspectiveCamera(
    perspective.fov || 75,
    perspective.aspect || window.innerWidth / window.innerHeight,
    perspective.near || 0.01,
    perspective.far || 1e3
  );
  camera.position.x = position.x;
  camera.position.y = position.y;
  camera.position.z = position.z;
  return camera;
};
export {
  DefaultCameraSettings
};
//# sourceMappingURL=kgengine_Engine_Cameras_DefaultCameraSettings__js.js.map
